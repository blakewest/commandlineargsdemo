This repo reproduces the error mentioned in https://github.com/75lb/command-line-args/issues/36

To produce the error, run the following in your terminal.

`npm install`

`node scriptA.js --withHelper`
